> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `enhancement`
- **State**: `ready-for-agent`
- **Parent PRD**: `docs/MEMBER-MESSAGING-PRD.md`

---

## What to build

Build the responsive frontend messaging dashboard, member profile message CTA button, trust & safety warning banners, and moderation queue integration.

- In `src/app/dashboard/messages/page.tsx`:
  - Split-view responsive layout (conversation sidebar + message pane).
  - Tabs: **Messages** (active threads) and **Requests** (pending message requests with Accept/Decline/Block CTAs).
  - Active conversation polling (3–5s interval when tab is focused and conversation open).
  - Trust & Safety Header Banner: *"🔒 Community Safety: Direct contacts are protected. Never share banking passwords, OTPs, or financial transactions in directory messages."*
  - Anti-fraud alert banner on messages flagged by heuristics.
  - 1-Click "Report Conversation" modal with categorized reasons and submission confirmation.
- In `src/app/directory/[id]/page.tsx`:
  - Add prominent **"💬 Send Message"** button opening the message composer.
- In `src/components/layout/TopNavBar.tsx` and `src/app/dashboard/page.tsx`:
  - Add unread message indicator badge linking to `/dashboard/messages`.
- In `src/app/admin/moderation/page.tsx`:
  - Add a **Reported Messages** tab displaying reported conversation snapshots with moderation resolution actions (Dismiss, Warn Member, Suspend Thread).
- In `tests/chat.test.mjs`:
  - Comprehensive end-to-end integration test suite verifying all 5 seams.

## Acceptance criteria

- [ ] `/dashboard/messages` renders fluidly across desktop and mobile viewports with heritage brand theme
- [ ] Directory profiles provide direct "Send Message" CTA for logged-in members
- [ ] Trust & Safety warning banners and anti-fraud alerts render prominently
- [ ] Admin Moderation Queue allows reviewing reported message threads with cryptographic snapshots
- [ ] Full test suite (`node --test tests/*.test.mjs`) and Next.js build (`npm run build`) pass 100%

## Blocked by

- docs/issues/014-chat-server-actions-and-anti-fraud.md
