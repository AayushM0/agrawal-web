> *This was generated by AI during triage.*
> State: `ready-for-agent`
> Category: `enhancement` / `backend` & `ux`
> Relates to: [Issue 031 - PRD](031-signup-draft-auto-save-and-serial-number-recycling.md)

# Issue 034: Rejection Lifecycle Resolution & "Update & Resubmit" Flow

## Objective
Prevent rejected applicants from being permanently blocked by "email already registered" errors. Enable them to view their rejection reason, load their previously submitted data, make corrections, and resubmit for administrative review without database unique key collisions.

## Scope
- Update `checkContactRegistration` in `src/actions/register.ts` to inspect application status:
  - Return `{ isRegistered: false, canResubmit: true, rejectionReason, previousHousehold }` when `status === 'rejected'`.
  - Return `{ isRegistered: true, isPending: true, householdCode }` when `status === 'pending_review'`.
- Update `registerHousehold` in `src/actions/register.ts` to handle rejected applicant resubmission:
  - If rejected, update the existing household record and replace members under transaction, resetting `status = 'pending_review'` and recording an audit trail.
- Update `/signup` UI to show the rejection reason banner and pre-fill fields when a rejected email is entered.

## Files to Modify
- `src/actions/register.ts`
- `src/lib/db.ts`
- `src/app/signup/page.tsx`
