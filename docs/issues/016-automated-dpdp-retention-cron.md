> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `enhancement`
- **State**: `ready-for-agent`

---

## What to build

Implement an automated DPDP Act 2023 compliance cron worker endpoint in `src/app/api/cron/prune-messages/route.ts`.

- In `src/app/api/cron/prune-messages/route.ts`:
  - Enforce Bearer token authorization matching `process.env.CRON_SECRET`.
  - Invoke `db.pruneExpiredMessages(90)` to delete non-flagged messages older than 90 days.
  - Return JSON metrics `{ success: true, prunedCount, executedAt }`.
- In `vercel.json` (if present or documentation):
  - Document the daily cron schedule (`0 3 * * *` - 3 AM UTC).

## Acceptance criteria

- [ ] Unauthenticated requests without `CRON_SECRET` return 401 Unauthorized
- [ ] Valid request successfully triggers `db.pruneExpiredMessages(90)` and returns pruned row count
- [ ] Automated test suite verifies endpoint security and pruning execution

## Blocked by

None - can start immediately
