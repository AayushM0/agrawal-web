> *This was generated by AI during triage.*
> State: `ready-for-agent`
> Category: `enhancement` / `backend`
> Relates to: [Issue 031 - PRD](031-signup-draft-auto-save-and-serial-number-recycling.md)

# Issue 033: Server-Side Registration Draft Storage & Sync via JSONB

## Objective
Allow cross-device resumption and persistent server backup of in-progress registration drafts by storing step form payloads in a `form_data JSONB` column on `registration_drafts`.

## Scope
- Add `form_data JSONB DEFAULT '{}'::jsonb` to `registration_drafts` in `src/db/schema.sql` and `ensureSchema()` in `src/lib/db.ts`.
- Update `saveRegistrationDraft` in `src/actions/draft.ts` to accept and persist `formData?: Record<string, any>`.
- Add `getRegistrationDraft` and `discardRegistrationDraft` server actions to query and clear server-side drafts.
- Ensure password data is stripped prior to database write.

## Files to Modify
- `src/db/schema.sql`
- `src/lib/db.ts`
- `src/actions/draft.ts`
