> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `enhancement`
- **State**: `ready-for-agent`

---

## What to build

Upgrade the household registration wizard (Step 1) to collect a secure password and verify identity exclusively via Email OTP.

- In `src/app/signup/page.tsx` (Step 1):
  - Add `Password` and `Confirm Password` fields with visibility toggle (eye icon).
  - Include interactive password strength indicator (visual requirements checklist: 8+ chars, upper, lower, number).
  - "Send Verification Code" triggers 6-digit Email OTP dispatched to primary email via Resend (`verify@maharajaagrasenfoundation.com`).
  - Retain phone number collection for directory contact info, but remove mobile SMS OTP requirement.
- In `src/actions/register.ts`:
  - Validate password meets complexity requirements before final insertion.
  - Hash password using `hashPassword(password)` (cost factor 12).
  - Store hash in `households.password_hash` and the Head's `members.password_hash`.

## Acceptance criteria

- [ ] Signup Step 1 displays password inputs with live strength feedback and visibility toggles
- [ ] Email verification code dispatches to user inbox from `verify@maharajaagrasenfoundation.com`
- [ ] Form prevents submission if passwords do not match or fail complexity rules
- [ ] Registered household in Supabase contains valid `password_hash` and never plaintext password
- [ ] Head of household member record contains matching `password_hash`

## Blocked by

- docs/issues/019-core-password-schema-and-bcrypt-engine.md
