> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Collocate Vercel Serverless Functions to Singapore (`sin1`) in `vercel.json` to match the physical region of the Supabase PostgreSQL database (`aws-0-ap-southeast-1.pooler.supabase.com`).

- Currently, `vercel.json` omits `"regions"`, causing Vercel to route serverless execution to `iad1` (Washington D.C., USA).
- Every query travels across the Pacific/Atlantic oceans, incurring 250ms+ per round-trip and ~1s for TLS handshakes.
- In `vercel.json`:
  - Add `"regions": ["sin1"]`
  - Ensure cron jobs and standard functions are targeted to `sin1`.

## Acceptance criteria

- [ ] `vercel.json` contains `"regions": ["sin1"]`
- [ ] Vercel deployments execute serverless functions in `sin1`
- [ ] Round-trip database latency drops from ~250ms to < 5ms

## Blocked by

- None - can start immediately
