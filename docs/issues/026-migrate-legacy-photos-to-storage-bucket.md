> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Migrate all 162 existing Base64 member and matrimony photographs currently stored in PostgreSQL to the public Supabase Storage bucket (`member-photos`), updating the database records to store lightweight public CDN URLs instead.

- In `scripts/migrate-photos.mjs`:
  - Run the migration against the production Supabase database.
  - For each member with `photo_url LIKE 'data:image/%'`:
    - Upload image buffer to `member-photos` bucket under `avatars/member_<id>-<timestamp>.<ext>`.
    - Retrieve public URL.
    - Update `members.photo_url` with the public URL.
  - Ensure zero data loss, MIME type preservation, and idempotency.

## Acceptance criteria

- [x] All 162 Base64 photographs are uploaded to the `member-photos` Supabase bucket
- [x] Database records in `members` reflect public HTTPS CDN URLs (`https://<ref>.supabase.co/...`)
- [x] 0 Base64 photos remain in the production database
- [x] All member avatars and matrimony candidate photos continue to display correctly in the frontend and PDF passes

## Blocked by

- None - can start immediately
