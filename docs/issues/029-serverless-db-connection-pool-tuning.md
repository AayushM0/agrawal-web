> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Tune the PostgreSQL connection pool settings in `src/lib/db.ts` for serverless environments to prevent connection exhaustion on Supabase's transaction pooler (`port 6543`).

- In `src/lib/db.ts`:
  - When running in production serverless lambdas, `max: 10` per container causes rapid exhaustion of Supabase connection limits under concurrent traffic.
  - Set `max: 2` (or `max: 1`) per container when running in production serverless, ensuring connections are released quickly and preventing queueing timeouts.
  - Reduce `connectionTimeoutMillis` to `5000` to fail fast instead of hanging for 10 seconds.

## Acceptance criteria

- [x] Production connection pool max is set appropriately for serverless (`max: 2`)
- [x] No connection timeouts or exhausted pool errors on Supabase pooler
- [x] Local development retains standard pool capacity for local testing
- [x] All 12/12 contract tests pass

## Blocked by

- None - can start immediately
