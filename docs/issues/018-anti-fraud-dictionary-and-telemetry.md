> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `enhancement`
- **State**: `ready-for-agent`

---

## What to build

Expand anti-fraud heuristics in `src/lib/anti-fraud.ts` with multi-lingual scam detection and external phishing URL warnings.

- In `src/lib/anti-fraud.ts`:
  - Add Devanagari / Hindi financial solicitation keywords (e.g. "पैसे भेजो", "खाते में ट्रांसफर", "लॉटरी", "ओटीपी शेयर").
  - Add suspicious URL shortener patterns (`bit.ly`, `tinyurl.com`, `t.me/`, `wa.me/`, unverified payment gateways).
  - Return detailed pattern categorization (`upi`, `bank_transfer`, `scam_keyword`, `suspicious_link`).
- In `src/actions/chat.ts`:
  - Log security telemetry when high-risk patterns or velocity limits are triggered.

## Acceptance criteria

- [ ] Hindi / Devanagari urgent transfer solicitations are flagged with safety alerts
- [ ] Suspicious shortener links and unverified payment URLs are flagged
- [ ] Unit tests in `tests/chat-actions.test.mjs` verify all new multi-lingual and link patterns

## Blocked by

- docs/issues/016-automated-dpdp-retention-cron.md
