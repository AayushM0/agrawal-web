> *This was generated by AI during triage.*

# Issue #1: Core Data Schema & Standard Serial Numbering (`MAFL-000-000-000`)
**Category:** `enhancement`  
**State:** `ready-for-agent`  
**User Story:** US-14  
**Blocked by:** None — can start immediately

## Agent Brief
### What to build
Update the PostgreSQL data layer and TypeScript types to support the institutional 9-digit serial number format (`MAFL-000-000-000`), address fields, and encrypted government identity storage (`aadhaar_number`, `pan_number`, `passport_number`, `govt_id_number`). Ensure generation logic produces unique serials and persists all new attributes without breaking existing queries.

### Touch Points
- `src/db/schema.sql`: Add migration columns for `households` and `members`.
- `src/types/household.ts`: Add `serialNo`, address fields, and identity fields to interfaces.
- `src/lib/db.ts`: Implement `generateSerialNumber()` returning `MAFL-XXX-XXX-XXX`, update `createHousehold`, `getHouseholds`, `getHouseholdById`, `getMemberById`.

### Acceptance Criteria
- [ ] Database schema includes `serial_no VARCHAR(32) UNIQUE`, address fields, and identity fields for `households` and `members`.
- [ ] New households automatically receive a structured serial number in the `MAFL-000-000-000` format.
- [ ] Persistence functions save and retrieve all new attributes cleanly with zero regressions.
- [ ] `node tests/seams.test.mjs` passes.
