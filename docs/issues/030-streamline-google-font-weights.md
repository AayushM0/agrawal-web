> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Trim Google Fonts weight preloads in `src/app/layout.tsx` from 10 distinct font files to the 3 essential weights actually utilized in the design system (`400`, `600`, `700`).

- In `src/app/layout.tsx`:
  - Poppins: reduce from `["300", "400", "500", "600", "700", "800", "900"]` to `["400", "600", "700"]`.
  - Noto Sans Devanagari: reduce from `["400", "600", "700", "800"]` to `["400", "600", "700"]`.
  - JetBrains Mono: keep `["400", "600"]`.
- Eliminates 6–7 `.woff2` font file preloads from the document `<head>`, accelerating First Contentful Paint (FCP) and Largest Contentful Paint (LCP).

## Acceptance criteria

- [x] Font weights in `src/app/layout.tsx` are streamlined to essential weights
- [x] Number of preloaded font requests in `<head>` drops from 10 to 4
- [x] Typography appearance across all headings, body text, and Devanagari script remains sharp and uncompromised
- [x] Next.js production build succeeds

## Blocked by

- None - can start immediately
