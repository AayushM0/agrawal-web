> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Code-split or dynamic-import the `country-state-city` library in `src/components/LocationSelector.tsx` to eliminate 2.4 MB of monolithic JSON data from the initial client bundle of `/signup` and `/dashboard`.

- In `src/components/LocationSelector.tsx`:
  - Replace top-level static import `import { Country, State, City } from "country-state-city"` with dynamic import or an API route / lazy loader that loads country/state/city datasets on demand when the user focuses or interacts with the input.
  - Or preload a lightweight curated list of primary community countries (India, Singapore, UAE, USA, UK, etc.) and fetch granular state/city lists asynchronously.
- Ensure the bundle size for `/signup` and `/dashboard` drops from ~2.4 MB to < 300 kB.

## Acceptance criteria

- [x] `country-state-city` is not included in the initial critical path client bundle
- [x] `/signup` First Load JS drops from 2.41 MB to < 400 kB
- [x] Location selection (Country, State, City, Pincode autocomplete) continues to function seamlessly with zero UX regression
- [x] Next.js production build completes with reduced chunk sizes

## Blocked by

- None - can start immediately
