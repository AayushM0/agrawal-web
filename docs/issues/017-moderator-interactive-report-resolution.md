> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `enhancement`
- **State**: `ready-for-agent`

---

## What to build

Implement interactive moderator resolution workflows for reported message threads in `src/actions/moderate.ts` and `src/app/admin/moderation/page.tsx`.

- In `src/actions/moderate.ts`:
  - Add `getMessageReports()`: Enforces admin role check (`session.role === 'admin'`) and returns all reported message tickets with cryptographic thread snapshots.
  - Add `resolveMessageReport({ reportId, action: 'dismiss' | 'warn' | 'suspend_chat', notes? })`:
    - Enforces admin role check.
    - Updates `message_reports.status` to `action_taken` or `dismissed`.
    - If `suspend_chat`, updates the relevant conversation status to `blocked`.
- In `src/app/admin/moderation/page.tsx`:
  - When `filter === 'reports'`, render the live list of message reports with:
    - Reporter Name & Household Code
    - Reported Member Name & Household Code
    - Stated Reason & Reporter Details
    - Thread Snapshot Viewer (showing the recent message history captured at report time)
    - Action Buttons: **Dismiss**, **Issue Warning**, **Suspend Chat** with real-time UI updates.

## Acceptance criteria

- [ ] Non-admin callers cannot access `getMessageReports()` or `resolveMessageReport()`
- [ ] Admin can view reported message details and message thread history
- [ ] Actions ("Dismiss", "Suspend Chat") update the report status and conversation state
- [ ] Automated tests verify admin-only protection and state transitions

## Blocked by

- docs/issues/016-automated-dpdp-retention-cron.md
