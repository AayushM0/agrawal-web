> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Eliminate the client-side Server Action waterfall in `MainHeader.tsx` by passing the authenticated session directly from server layout / server components into `MainHeader` as a prop (`initialSession`).

- Currently, `MainHeader` is `"use client"` and fires a POST request (`getSession()`) on every mount and window focus, creating an `isLoading: true` blank state and network latency.
- In `src/app/layout.tsx`:
  - Read session from cookies server-side via `getSession()` and pass as `initialSession` to `<MainHeader initialSession={session} />`.
- In `src/components/layout/MainHeader.tsx`:
  - Initialize `session` state with `initialSession`, rendering the user's correct login status immediately with zero client-side network round-trips.

## Acceptance criteria

- [x] Header renders authenticated / unauthenticated state immediately on HTML load
- [x] No client-side `POST` request waterfall for `getSession()` on page mount
- [x] Session transitions (login/logout) continue to update smoothly
- [x] All 12/12 contract tests pass

## Blocked by

- None - can start immediately
