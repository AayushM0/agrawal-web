> *This was generated by AI during triage.*

## Triage Metadata
- **Category**: `performance`
- **State**: `ready-for-agent`

---

## What to build

Eliminate the execution of ~200 lines of DDL statements (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`) during serverless cold starts in `src/lib/db.ts`.

- In `src/lib/db.ts`:
  - Currently, `ensureSchema(client)` executes on every new serverless container initialization where `globalForPg.schemaEnsured` is false.
  - In PostgreSQL, DDL commands acquire `ACCESS EXCLUSIVE` table locks, freezing incoming queries for 1.5s–3s.
  - In production, the schema is already completely migrated and permanent.
  - Gate `ensureSchema()` so it only runs when `process.env.NODE_ENV === "development"` or if `AUTO_MIGRATE_SCHEMA === "true"`.
  - In production (`process.env.NODE_ENV === "production"`), skip runtime DDL entirely.

## Acceptance criteria

- [ ] `ensureSchema()` is bypassed on production serverless cold starts
- [ ] Local development retains automatic schema initialization if needed
- [ ] No `ACCESS EXCLUSIVE` table lock contention on production container startup
- [ ] All 12/12 contract tests pass

## Blocked by

- None - can start immediately
